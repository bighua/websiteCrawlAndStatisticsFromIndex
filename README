实时数据统计
此模块提供两个功能：
1.定时统计索引数据
根据配置文件（conf/jcm.properties）的配置，定时统计某个类型（新闻，图片，视频，口碑）的某个维度（网址，车型，网址+车型，等）的数据，
以自然日为单位生成数据文件。详细内容请参照wiki。
2.索引数据访问接口
数据访问接口通过jcm-rmi组件（http://source.8drive.cn/jcm-component/jcm-rmi/wikis/home）服务端提供，使用方法请参照wiki。